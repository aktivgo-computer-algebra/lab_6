import string

from polynom.config import PolynomialConfig

class Polynomial:
    def __init__(self, config: PolynomialConfig, polynomial: str):
        self.config = config
        self.polynomial = self.__parse_from_string(polynomial)

    def __parse_from_string(self, polynomial: str) -> []:
        result = []



        return result
